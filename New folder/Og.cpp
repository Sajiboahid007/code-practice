#include<bits/stdc++.h>
using namespace std;
int main()
{
    while(true)
    {
        int L,R;
        scanf("%d %d",&L,&R);
        if(L == 0 && R == 0 ) break;
        printf("%d\n",L+R);
    }
    return 0;
}
